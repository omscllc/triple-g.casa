repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.2.0 # Use the latest stable version
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        additional_dependencies: ['gibberish-detector']

